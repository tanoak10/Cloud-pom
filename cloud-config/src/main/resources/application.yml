spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tanoak10/cloud-config.git
          default-label: master
          search-paths: consul
server:
  port: 1201
